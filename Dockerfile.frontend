# 使用 Node.js 作为基础镜像
FROM node:16-alpine

# 设置工作目录
WORKDIR /app

# 复制前端项目文件
COPY frontend/ ./

# 安装依赖
RUN npm install

# 暴露端口
EXPOSE 5173

# 启动前端开发服务器
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]